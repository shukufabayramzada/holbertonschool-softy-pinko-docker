# Use the latest Ubuntu as the base image
FROM ubuntu:latest

# Update and upgrade APT packages
RUN apt-get update && apt-get upgrade -y

# Set the command to echo "Hello, World!" when the container is run
CMD ["echo", "Hello, World!"]
